## 1. Archive 状态解耦

- [x] 1.1 调整 `GameEarthApp` 与 `GameArchiveView` 的 archive 接线，保留 `mainViewMode`、入口和返回回调，但移除 archive 选择对 Earth game-selection action 的依赖。
- [x] 1.2 在 archive 边界内建立 `openYearKey` 与 `selectedArchiveGameId` 的权威状态，并以独立 unknown-year key 区分未知年份和关闭状态。
- [x] 1.3 添加回归测试，证明浏览已知国家和 `UNKNOWN` 游戏前后 Earth 的国家、区域、游戏、镜头模式及 selection revision 均不变化，pathname 仍保持 `/`。

## 2. 数据与派生模型整理

- [x] 2.1 建立纯 archive model 模块，集中实现搜索规范化、筛选选项、结果过滤、年份分组、代表作品、预览封面、平均评分、结果统计和有效选择解析。
- [x] 2.2 实现并记录年份索引始终倒序、unknown-year 末位，以及 `馆藏评分优先` 仅重排年度内游戏并使用标题/ID tie-break 的规则。
- [x] 2.3 实现筛选后年份或游戏失效的确定性回退，以及零结果时清除过期年度和详情内容的规则。
- [x] 2.4 添加 Node 测试覆盖 17 个当前年份、平均评分、代表作品、搜索字段、同类 OR/跨类 AND、排序稳定性、unknown-year key 和快速连续输入的最终结果。

## 3. 桌面端阅读室结构

- [x] 3.1 将 archive 重组为紧凑品牌栏、工具栏、左侧年份索引、中部年度主展页和右侧年度馆藏，并保留真实封面与详情入口。
- [x] 3.2 使用视口约束和内部滚动边界，使 1366×768、1440×900、1920×1080、1280×720 首屏无需 document scroll 即可操作年份、查看当前年/封面、进入工具和打开详情。
- [x] 3.3 建立 Chronicle Reading Room 的 archive 根级 token、排版、纸张/印刷纹理和差异化内容层级，移除主层级中的黑绿荧光终端与同质圆角描边卡片表现。

## 4. 移动端阅读流

- [x] 4.1 为 390×844 实现紧凑顶部栏、吸顶年份导航、当前年度主展、纵向馆藏列表、筛选面板入口和移动详情层，不复用缩小后的桌面三栏布局。
- [x] 4.2 为移动年份横向手势设置滚动包含和 touch-action 边界，确保纵向 document scroll 不被截获且页面无横向溢出。
- [x] 4.3 添加移动浏览器断言，验证所有主控件触控尺寸、年度内容、长标题、馆藏列表和详情层均不裁切或遮挡。

## 5. 年份导航

- [x] 5.1 将年份条目实现为带可见焦点和唯一当前语义的原生按钮，并以标签和程序关联说明导航用途、当前位置和对应年度内容。
- [x] 5.2 实现与布局方向一致的 Arrow 键、Home、End、Enter、Space、roving tab index 及聚焦项可见性行为。
- [x] 5.3 实现新/旧年份边界禁用状态和触控/鼠标共用的选择路径，并添加键盘、边界和快速年份切换测试。

## 6. 搜索、筛选、排序与空状态

- [x] 6.1 将标题/中文标题/开发商/发行商搜索、类型与平台多选、排序和结果数量接入统一派生模型。
- [x] 6.2 为桌面提供紧凑工具栏，为移动提供可访问筛选面板；两者共享同一控制状态、选项和重置动作。
- [x] 6.3 实现清晰的零结果状态，确保旧年份、旧封面和旧详情不会残留，并提供恢复全部结果的操作。
- [x] 6.4 添加搜索、组合筛选、两个排序模式、重置和空状态的单元/浏览器测试。

## 7. 游戏详情交互

- [x] 7.1 重构 `ArchiveYearModal`/`ArchiveDossier` 为 archive 专属 portal dialog，使游戏 ID 直接解析到 archive 结果而不经过国家映射。
- [x] 7.2 实现初始焦点、焦点陷阱、背景不可交互、Escape/显式关闭、内部长内容滚动和触发游戏焦点恢复。
- [x] 7.3 修复同一详情层内切换游戏时的选中语义与内容同步，并避免每次选中变化都把焦点重置到关闭按钮。
- [x] 7.4 添加覆盖当前 992 条游戏（包括当前 200 条 `UNKNOWN`）的选择解析测试，以及详情字段、Escape、焦点陷阱和焦点恢复的 Playwright 测试。

## 8. 图片与主视觉资产

- [x] 8.1 复用现有封面来源和集中 fallback，为代表封面与馆藏封面补充稳定比例、intrinsic 尺寸、响应式 display size、适当的 lazy/priority 和失败后同几何降级。
- [x] 8.2 在 `public/images/archive/` 的资产说明中记录原创无文字主视觉的主题、暖色材质、全球游戏文化氛围、禁用文字/Logo、目标尺寸、焦点、桌面/移动安全裁切、DOM 文案分离、加载优先级、CSS fallback 和 WebP/AVIF ≤350 KB 预算。
- [x] 8.3 在人工确认资产说明后，才在另行授权的实现步骤中生成、压缩并接入正式主视觉；未获确认时保持可交付的 CSS/纸张 fallback。
- [x] 8.4 添加封面加载失败、几何预留、首屏 priority 数量和移动溢出的浏览器验证。

## 9. 动画与 Reduced Motion

- [x] 9.1 将 hover、focus、年份选择等反馈限制为短 CSS 过渡，仅在确有空间连续性收益时复用现有 Motion 处理筛选或详情层。
- [x] 9.2 从新 archive 路径移除不再需要的延迟 GSAP intro 初始化，确保离开 archive 后不会对已卸载节点启动或完成动画。
- [x] 9.3 实现 `prefers-reduced-motion: reduce` 静态等价表现，关闭平滑滚动、装饰入场和连续动画，并添加 reduced-motion 浏览器测试。

## 10. 可访问性与样式隔离

- [x] 10.1 审核并补齐返回、搜索、筛选、排序、年份、游戏卡、详情和空状态的名称、角色、状态、错误/结果提示与焦点样式。
- [x] 10.2 将新视觉规则放入 archive CSS Module 或唯一 archive 根作用域，portal 使用 archive 专属命名空间；只移除已证实由新实现替代的 archive 全局规则。
- [x] 10.3 添加选择器范围检查和键盘-only 冒烟测试，确认 archive 样式不匹配 hub/Earth 的通用 `button`、`h1`、`img`、`.card` 或 `.panel` 元素。

## 11. 自动化测试

- [x] 11.1 新增 archive model/state Node 测试套件并固定 992 条目录、`UNKNOWN` 选择、派生年份、筛选、排序和失效回退契约。
- [x] 11.2 新增 Game Chronicle Playwright 套件，覆盖入口/返回/pathname、年份、搜索、筛选、排序、空状态、详情、键盘、焦点、图片失败、reduced motion 和 console error/warning。
- [x] 11.3 扩充 hub/Earth 回归，证明 archive 浏览不改变 Earth reducer/镜头状态且首页与地球入口、布局和交互保持原状。

## 12. 文档与资产记录

- [x] 12.1 更新 `docs/01_PRODUCT_SPEC.md`，记录 Chronicle Reading Room 的用户流程、桌面/移动行为和明确 non-goals。
- [x] 12.2 更新 `docs/02_FEATURE_MAP.md`，登记 archive model、组件、样式、测试和资产说明的最终文件位置与责任。
- [x] 12.3 更新 `docs/03_ARCHITECTURE.md`，记录 `mainViewMode` 保留、archive 状态所有权、与 Earth reducer 的隔离边界及派生模型。
- [x] 12.4 复核 `docs/00_PROJECT_INDEX.md`，仅在新增的重要文件类别需要索引时更新，并确认未把历史 Spec Kit 文档当作活动规范修改。

## 13. 浏览器验证与质量门禁

- [x] 13.1 在 1366×768、1440×900、1920×1080、1280×720 运行测量脚本，记录根节点、masthead、工具栏、年份导航、年度主展、馆藏和详情入口的 `getBoundingClientRect()`，确认首屏接受标准。
- [x] 13.2 在 390×844 测量 `scrollWidth/clientWidth`、`scrollHeight/clientHeight`、吸顶年份导航和详情层，确认无 document 横向溢出、滚动冲突、裁切或文字遮挡。
- [x] 13.3 在所有验收视口检查 console error、warning、hydration、图片失败和快速年份/筛选切换；临时截图、日志和输出保持未跟踪且不提交。
- [x] 13.4 运行 `npm run lint`、`npm run typecheck`、`npm test`、`npm run build` 和相关 `npm run test:e2e`，修复本 Change 引入的问题并确认 homepage、Earth、archive 全部通过。
- [x] 13.5 最终检查 Git diff 只包含批准的 archive、接线、测试、资产说明和必要 living documentation 文件，且未修改路由、数据结构、RAWG 流程、Earth reducer 语义、首页视觉或 backup 分支。

## 14. 图片性能基线

- [x] 14.1 代码审计当前年度馆藏的挂载数量、原始图片路径、本地/远程比例和大文件风险，并记录当前实现是否挂载整年封面。
- [x] 14.2 使用真实浏览器在固定视口和缓存条件下记录优化前首次进入、切换一年和滚动馆藏的图片请求数量、传输体积、来源与 mounted image 数量。

## 15. 当代游戏档案馆视觉精修

- [x] 15.1 将顶栏、工具、年份索引、年度主展、馆藏和详情从厚重直角框调整为现代 sans 控件、hairline、适度圆角、分层表面与克制阴影，同时保留暖米白/炭黑/暗红/黄铜和 serif 年份标题。
- [x] 15.2 强化选中年份与游戏的对比、景深和视觉张力，并为按钮、筛选、评分、标签、hover 抬升和封面缩放建立一致的现代交互细节。

## 16. 数据驱动年度视觉

- [x] 16.1 使用共享轻量氛围底图加 active year 代表作品与评分靠前 2–3 张真实封面的 DOM 景深拼贴，不生成 17 张风格不一致的 AI 图片。
- [x] 16.2 为 2010–2026 每年派生不同的封面 ID 签名、构图 variant、裁切、位置和强调色，并保持年份/游戏名为真实 DOM 文本。
- [x] 16.3 为氛围图或年度封面失败实现同几何纸张/网点/品牌占位降级，并更新资产说明记录方案、裁切和成本。

## 17. 年份与详情动画

- [x] 17.1 用现有 Motion/CSS 实现 180–360ms 的年份方向感、主视觉淡入景深缩放、代表作品与馆藏协调过渡及受限 stagger。
- [x] 17.2 为 hover 与详情抽屉建立轻抬升、封面缩放和流畅进出，并确保快速年份切换以最终意图为准、无旧数据回跳。
- [x] 17.3 在 reduced motion 下取消位移、缩放和 stagger，只保留即时状态变化，并补充动画/快速切换测试。

## 18. ArchiveCover 与渐进加载

- [x] 18.1 建立统一 `ArchiveCover` 组件，提供稳定尺寸、响应式 sizes、Next Image 优化、skeleton、加载状态和统一失败占位。
- [x] 18.2 年度馆藏初始只渲染 8 张，使用 IntersectionObserver 以有界批次追加后续游戏；年份切换时同步重置，避免进入页面挂载全部年度或整年高清封面。
- [x] 18.3 当前年代表作品和前 6–8 张馆藏优先加载，其余 lazy load；对邻近年份意图最多预取 3 张优化缩略图，不批量预取原图。
- [x] 18.4 添加测试覆盖 skeleton、失败占位、初始 mounted 数、滚动追加、年份切换重置、有限预取和布局稳定性。

## 19. 性能复测与文档

- [x] 19.1 在相同浏览器条件下记录优化后首次进入、年份切换和滚动的请求数/传输体积，并与 14.2 基线对比。
- [x] 19.2 更新功能地图、架构、产品说明和 archive 资产说明，记录 Contemporary Game Archive、动态年度拼贴和渐进图片策略。
