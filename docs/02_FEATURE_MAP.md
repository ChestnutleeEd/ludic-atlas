# 02_FEATURE_MAP.md

## Purpose

This document maps each product feature to its responsible files.

Codex must read this file before modifying any feature. The goal is to avoid unnecessary full-project search and quickly locate the correct files.

## How Codex Should Use This File

Before modifying a feature:

1. Find the feature in the table below.
2. Read only the listed main file and related files.
3. Modify the smallest necessary file set.
4. If a feature or file path changes, update this document.

If the requested feature is not listed here, Codex may inspect the project structure and then add the new mapping to this file.

## Feature-to-File Mapping

| Feature              | User-Facing Behavior                                                  | Main File                                      | Related Files                                                                                                                         | Notes                                              |
| -------------------- | --------------------------------------------------------------------- | ---------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------- |
| Project setup        | App can be installed, linted, type checked, and run locally           | `package.json`                                 | `next.config.ts`, `tsconfig.json`, `postcss.config.mjs`, `eslint.config.mjs`, `package-lock.json`, `.gitignore`, `start-mac.command`, `start-windows.bat`                       | Next.js + TypeScript + Tailwind + ESLint baseline. Release helper scripts let downloaded source packages install dependencies and start the local app with a double click when Node.js is available. |
| Quality and performance verification | Pure data, atomic selection, projection state, active-only renderer mounting, cancellable camera, dynamic markers, LOD fallback, responsive layout, rapid switching, accessibility, production frame delivery, and browser rendering are regression-tested | `tests/core-data.test.ts` | `tests/cover-size-contract.test.ts`, `tests/marker-contracts.test.ts`, `tests/marker-geography-phase3.test.ts`, `tests/e2e/earth-marker-phase1.spec.ts`, `tests/e2e/marker-lod.spec.ts`, `tests/e2e/globe-visual-phase4.spec.ts`, `tests/e2e/earth-atmosphere-phase5.spec.ts`, `tests/e2e/earth-phase6-validation.spec.ts`, `tests/earth-view-state.test.ts`, `tests/explorer-state.test.ts`, `tests/camera-intent.test.ts`, `tests/safe-viewport.test.ts`, `tests/globe-markers.test.ts`, `tests/geo-markers.test.ts`, `tests/e2e/earth-explorer.spec.ts`, `tests/e2e/globe-stability.spec.ts`, `docs/EARTH_EXPLORER_VALIDATION.md`, `playwright.config.ts`, `package.json` | Node tests cover cover-size normalization/storage, marker identity/reconciliation and interaction policy alongside the existing geography, layout, cache, and state contracts. The focused marker suite verifies in-motion visibility, last-known layouts, retained DOM/image identity, request deduplication, size controls/persistence, 390 px safety, bounded renderer ownership, France 10–18 / Poland 6–12 default-density contracts, truthful overflow, Belgium aggregation, Japan multi-island placement, and one concise Global/France/Poland frame/long-task sample. The accepted serial browser baseline is Earth 63/63 and complete project 80/80 with zero skipped tests. |
| Project documentation | User and Codex can find product positioning, setup, source entrypoints, release notes, and verification rules | `README.md`                                    | `docs/assets/readme/`, `docs/releases/`, `docs/00_PROJECT_INDEX.md`, `docs/06_CODEX_RULES.md`, `.codex_task_template.md`, `docs/05_TASK_LOG.md`                                | Keep source location, README preview screenshots, release documentation, and page acceptance guidance current |
| Historical specification archive | Previous Earth Explorer requirements, plans, research, tasks, and acceptance records remain available for traceability | `docs/archive/spec-kit/001-optimize-earth-experience/spec.md` | `docs/archive/spec-kit/001-optimize-earth-experience/` | Read-only historical material from the retired Spec Kit workflow; it is not an active source of implementation instructions. |
| Home page            | User opens the app and sees the Ludic Atlas / 游戏星图 single-screen desktop landing hub | `src/app/page.tsx` | `src/components/GameEarthApp.tsx`, `src/components/home/LandingHub.tsx`, `src/app/globals.css`, `tests/e2e/homepage.spec.ts` | Keep page entry simple; the hub preserves local `hub / earth / archive` switching and does not add route state |
| Root layout          | Provides App Router document shell, Ludic Atlas metadata, favicon, and global CSS | `src/app/layout.tsx`                           | `src/app/globals.css`, `public/favicon.svg`                                                                                           | Keep layout minimal                                |
| Main product shell   | Overall layout, top-level state, data flow                            | `src/components/GameEarthApp.tsx`              | `src/lib/explorerState.ts`, `src/lib/earthViewState.ts`, `src/types/earth.ts`, `src/data/gameCatalog.ts`, `src/lib/filterGames.ts`, `src/components/earth/EarthProjectionViewport.tsx`, `src/components/home/LandingHub.tsx`, `src/components/globe/GameGlobe.tsx`, `src/components/archive/GameArchiveView.tsx` | Owns one atomic Earth selection reducer, shared Earth filters, `projectionMode`, and settled renderer-view compatibility snapshots. Current delivery defaults to and exposes Globe only; Atlas remains an inert future contract. Archive receives only catalog data and return callback and cannot dispatch Earth selection actions. |
| Earth renderer contract | Production UI mounts one Globe renderer while retaining an active-only future-renderer boundary | `src/components/earth/EarthProjectionViewport.tsx` | `src/components/GameEarthApp.tsx`, `src/lib/earthViewState.ts`, `src/types/earth.ts`, `tests/earth-view-state.test.ts`, `tests/e2e/earth-explorer.spec.ts`, `docs/DEFERRED_ATLAS_MAP_PLAN.md` | `globe` mounts `GameGlobe`; the retained internal `atlas` placeholder has no canvas, WebGL, animation loop, geography request, or long-lived listener and is not a user-facing destination. Formal Atlas work resumes only through a future `add-atlas-map-renderer` change. |
| Deferred Atlas Map plan | Preserves the future 2D/2.5D product, OrthographicCamera architecture, projection/antimeridian decisions, shared contracts, requirements, tasks, prerequisites, and validation handoff | `docs/DEFERRED_ATLAS_MAP_PLAN.md` | `src/components/earth/EarthProjectionViewport.tsx`, `src/types/earth.ts`, `src/lib/earthViewState.ts`, `src/lib/geographyRepository.ts`, `src/lib/markerLayout.ts` | Planning reference only; Atlas is not part of current `redesign-earth-explorer` acceptance. Recommended future branch/change: `feat/add-atlas-map-renderer` / `add-atlas-map-renderer`. |
| Landing hub         | User chooses between Earth Explorer / 地球探索 and Game Chronicle / 游戏编年馆 through complete asymmetric entrance surfaces | `src/components/home/LandingHub.tsx` | `src/components/GameEarthApp.tsx`, `src/app/globals.css`, `public/images/home/earth-explorer-archive.webp`, `public/images/home/game-chronicle-archive.webp`, `public/images/home/README.md`, `tests/e2e/homepage.spec.ts` | Desktop uses a scoped `100dvh` world-game-archive composition with an approximately 58:42 Earth-led hierarchy, integrated collection count / year range, original text-free responsive imagery, one lead-image preload, fallback backgrounds, restrained motion, and no vertical scroll from 1366×768 upward. Mobile stacks both entrances and scrolls naturally. |
| Game Chronicle view | User browses all generated global games through the Contemporary Game Archive, with chronological year index, data-driven annual cover stage, progressive annual collection, compact tools, and game dossier | `src/components/archive/GameArchiveView.tsx` | `src/components/archive/ArchiveCover.tsx`, `src/components/archive/useArchiveReducedMotion.ts`, `src/components/archive/GameArchiveView.module.css`, `src/components/archive/ArchiveTimeline.tsx`, `src/components/archive/ArchiveYearModal.tsx`, `src/components/archive/ArchiveDossier.tsx`, `src/lib/archiveModel.ts`, `src/components/GameEarthApp.tsx`, `src/data/games.ts`, `src/lib/gameCover.ts`, `src/lib/localization.ts`, `next.config.ts`, `public/covers/rawg/`, `public/images/archive/chronicle-reading-room.webp`, `public/images/archive/README.md`, `tests/archive-model.test.ts`, `tests/e2e/game-archive.spec.ts` | Archive owns `openYearKey` and `selectedArchiveGameId`; no selection flows through the Earth reducer, including `UNKNOWN` country records. The pure model derives filters, chronological groups, three-cover annual signatures, representatives, averages, counts and valid fallbacks. Each 2010–2026 stage uses untouched real covers over one shared atmosphere layer. `ArchiveCover` resolves the existing all-catalog local WebP cache and provides Next Image display variants, stable 2:3 geometry, skeleton/fallback, eight-item bounded collection batches, and limited three-cover year-intent prefetch. Desktop is a bounded first-viewport workspace with a right dossier drawer; 390px mobile uses sticky horizontal years, vertical content and a full-screen dossier. Scoped contemporary styling, direction-aware Motion, focus management and reduced motion are part of the feature contract. `ArchiveYearDrawer.tsx` remains unused legacy code. |
| Legacy 2.5D earth map view  | File remains available, but there is no active UI entry in the MVP shell | `src/components/globe/GameGlobe2D.tsx`         | `src/lib/geo.ts`, `src/components/globe/GameTooltip.tsx`, `src/app/globals.css`, `public/data/mock-countries.geojson`, `src/data/countries.ts`, `src/data/games.ts` | Legacy fallback component is retained on disk to avoid wide deletion risk, but `GameEarthApp` no longer imports or renders it and the active globe UI no longer exposes a 2.5D / 3D toggle |
| 3D earth / map view  | User sees, deeply zooms, manually rotates, filters by region, clicks the globe surface, and focuses a cinematic real interactive 3D earth with LOD country outlines | `src/components/globe/GameGlobe.tsx`           | `src/components/GameEarthApp.tsx`, `src/app/globals.css`, `src/lib/globeBoundary.ts`, `src/lib/geographyRepository.ts`, `src/lib/geography.ts`, `src/lib/globeCamera.ts`, `src/lib/globeNavigation.ts`, `src/lib/safeViewport.ts`, `public/data/earth-lod/`, `public/images/earth/`, `tests/globe-phase4-repair.test.ts`, `tests/e2e/globe-phase4-repair.spec.ts`, `tests/e2e/earth-atmosphere-phase5.spec.ts` | Global, region and selected-country resources load progressively through a deduplicated repository; failed detail retains lower LOD. Every Polygon/MultiPolygon ring is rendered independently through bounded spherical interpolation. `GameGlobe` owns the user-approved decorative atmosphere layer: responsive Earth-only WebP files sit above the opaque stage backing but below chart/canvas/UI layers, with fixed intrinsic geometry, a left-biased crop, central/right safe zones, error self-hiding, and a complete CSS fallback. Runtime index sampling remains removed and camera/active-only contracts remain unchanged. |
| Game cover markers   | Country aggregate dots and selected/high-rating game covers remain visible through Earth interaction | `src/components/globe/GameMarkers.tsx`         | `src/components/globe/GameGlobe.tsx`, `src/lib/geography.ts`, `src/lib/markerLayout.ts`, `src/lib/globeMarkerModel.ts`, `src/lib/markerContracts.ts`, `src/lib/gameCover.ts`, `src/components/globe/GameTooltip.tsx`, `tests/marker-contracts.test.ts`, `tests/e2e/earth-marker-phase1.spec.ts`, `tests/e2e/globe-phase4-repair.spec.ts` | Semantic/layout identities reconcile retained descriptors in place. Drag, wheel, automatic rotation, camera tools, and programmatic flight keep the last accepted layout visible; the low-detail tier removes only decoration and pointer work while back-face opacity remains owned by `htmlElementVisibilityModifier`. Settled collision publishes atomically, Belgium remains truthfully aggregated, Japan remains component-aware, and selection/class/ARIA/overflow changes preserve retained button/image nodes and loaded requests. |
| Game hover tooltip   | User hovers over a game and sees title, rating, year, genre           | `src/components/globe/GameTooltip.tsx`         | `src/types/game.ts`, `src/components/globe/GameMarkers.tsx`, `src/lib/localization.ts`                                                | Tooltip receives hovered game data from props and displays Chinese-first copy |
| Right panel wrapper  | An on-demand drawer switches between region-scoped country overview and selected-country detail | `src/components/panels/RightPanel.tsx`         | `src/components/panels/CountryPanel.tsx`, `src/components/panels/CountryDetailPanel.tsx`, `src/components/panels/GameDetailCard.tsx`, `src/lib/regions.ts`  | Desktop keeps the searchable country/detail drawer off-canvas and inert until requested or a country is selected, then slides it over the globe without shrinking the canvas. When `selectedGame` exists, it resets retained panel scroll, renders one independently scrollable modal detail layer above the panel, marks background content inert, and supports Escape close so the detail header cannot be clipped after choosing a lower gallery card. On mobile the same panel is a fixed three-state bottom sheet (`collapsed`, `peek`, `expanded`) with explicit 68px collapsed height, a drag handle, state buttons, internal scrolling, and globe-drag collapse behavior. |
| Country list panel   | User sees compact searchable country rows with game count, average rating, and top genre | `src/components/panels/CountryPanel.tsx`       | `src/lib/stats.ts`, `src/lib/localization.ts`, `src/data/games.ts`, `src/data/countries.ts`                                           | Statistics are memoized from the filtered game list and displayed in Chinese-first compact rows. Search supports Chinese names, English names, and country codes; row clicks update selected country and trigger globe focus through app state |
| Global RAWG gallery  | User can preview batch-generated RAWG games and cached covers before choosing a country | `src/components/panels/GlobalGameGallery.tsx`  | `src/components/panels/RightPanel.tsx`, `src/types/game.ts`, `src/lib/localization.ts`, `src/data/games.generated.ts`, `public/covers/rawg/` | Appears below the country overview only when no country is selected. Uses regular `<img>` for generated `coverImage` paths; limits the preview to the first 24 games |
| Country detail panel | User clicks a country and sees a vertical country detail panel with stats and game cards | `src/components/panels/CountryDetailPanel.tsx` | `src/lib/filterGames.ts`, `src/lib/stats.ts`, `src/lib/localization.ts`, `src/lib/gameCover.ts`, `src/components/panels/GameDetailCard.tsx`                   | Uses selected country, current year-filtered games, selected game state, and Chinese-first labels. Includes return-to-overview, country stats, up to 12 current-year game cards with stable cover fallback, and a sticky bottom game summary dock |
| Game detail card     | User clicks a game and sees a closable compact description card        | `src/components/panels/GameDetailCard.tsx`     | `src/types/game.ts`, `src/lib/localization.ts`, `src/lib/gameCover.ts`, `src/data/games.ts`, `public/covers/fallback-game-cover.svg`                                                                   | Shows reusable compact game details with Chinese-first labels, real or fallback cover image, developer / publisher / year / genre / platform / rating / description, and optional close action |
| Bottom controls      | User opens a compact tray for filters, marker size, camera mode, rotate mode, and display options  | `src/components/controls/BottomControls.tsx`   | `src/components/controls/YearSlider.tsx`, `src/components/controls/CoverSizeSlider.tsx`, `src/components/controls/ViewModeToggle.tsx`, `src/lib/regions.ts` | Starts as a small bottom-right summary and expands only on request. The expanded tray shows current game/country/region status, year slider, marker size, Overview / Surface toggle, rotate toggle, and marker display mode; camera zoom/reset/focus stay in the map-local tool rail. |
| Year filter          | User filters games by release year                                    | `src/components/controls/YearSlider.tsx`       | `src/lib/filterGames.ts`, `src/components/GameEarthApp.tsx`                                                                           | Uses native range inputs; state owned by GameEarthApp |
| Cover size control   | User changes and restores the canonical cover height                  | `src/components/controls/CoverSizeSlider.tsx`  | `src/components/controls/BottomControls.tsx`, `src/lib/coverSize.ts`, `src/components/globe/GameGlobe.tsx`, `src/components/GameEarthApp.tsx`, `tests/cover-size-contract.test.ts`, `tests/e2e/earth-marker-phase1.spec.ts` | Earth uses a direct 48–112 px height contract with a 72 px default and 4 px step. The native slider, decrease/increase buttons, px readout, keyboard/ARIA/focus states, versioned `ludic-atlas:earth-cover-size:v1` recovery, CSS live sizing, bucketed RAF layout, final commit and hysteresis are scoped to Earth; Hub and Chronicle remain unaffected. |
| View mode toggle     | User switches marker display mode                                     | `src/components/controls/ViewModeToggle.tsx`   | `src/components/globe/GameMarkers.tsx`, `src/components/GameEarthApp.tsx`                                                             | Supports countries and games modes                 |
| Game data export     | Provides representative game records to the frontend from local static modules | `src/data/games.ts`                            | `src/data/games.generated.ts`, `src/data/games.mock.ts`, `src/types/game.ts`, `src/lib/localization.ts`, `public/covers/README.md`, `public/covers/rawg/`, `docs/04_DATA_SCHEMA.md` | `games.ts` is the stable frontend import. Generated RAWG data is preferred through `games.generated.ts`; `games.mock.ts` preserves the original stable mock dataset. RAWG generated `coverImage` values may point to cached `/covers/rawg/...` files after `npm run data:covers` |
| Indexed game catalog | Gives runtime views shared O(1) game / country lookup, grouped country games, recognition counts, and total statistics | `src/data/gameCatalog.ts` | `src/data/games.ts`, `src/data/countries.ts`, `src/lib/stats.ts`, `src/components/GameEarthApp.tsx`, `tests/core-data.test.ts` | Builds immutable-facing indexes once from local static data; selection and country-list callers should use it instead of repeating full-array `find` / `filter` scans. |
| RAWG data generation | Local script fetches RAWG data and writes a static generated game data module | `scripts/fetch-rawg-games.mjs`                 | `scripts/rawg-seeds.mjs`, `src/data/games.generated.ts`, `src/data/games.ts`, `package.json`, `.env.local`, `README.md`, `docs/03_ARCHITECTURE.md`, `docs/04_DATA_SCHEMA.md` | Browser components must not call RAWG. The script reads `RAWG_API_KEY` from `.env.local`; `RAWG_FETCH_MODE=batch` uses the RAWG `/api/games` list endpoint with date / metacritic pagination and writes `src/data/games.generated.ts`. Non-batch mode preserves the legacy seeded detail-fetch flow through `scripts/rawg-seeds.mjs` |
| RAWG cover cache     | Local script downloads generated remote RAWG cover URLs into Git-tracked static assets and rewrites `coverImage` to local paths | `scripts/cache-rawg-covers.mjs` | `src/data/games.generated.ts`, `public/covers/rawg/`, `package.json`, `docs/04_DATA_SCHEMA.md`, `docs/03_ARCHITECTURE.md` | `npm run data:covers` reads generated data, uses `undici` proxy support from shell proxy env vars, downloads covers with limited concurrency, skips existing non-empty files, keeps failed URLs unchanged, writes `/covers/rawg/...` paths back to `games.generated.ts`, and does not read `.env.local` or require a RAWG API key |
| RAWG cover compression | Local script compresses cached RAWG cover files to WebP and rewrites generated cover references | `scripts/compress-rawg-covers.mjs` | `src/data/games.generated.ts`, `public/covers/rawg/`, `package.json` | `npm run data:covers:compress` creates a timestamped backup of `games.generated.ts` plus RAWG cover file statistics under `backups/`, converts jpg/jpeg/png/webp covers in `public/covers/rawg/` to `.webp` with max dimension 1000px and quality 80, deletes old same-basename jpg/jpeg/png files after successful conversion, and verifies generated local cover references exist |
| Country inference apply | Writes reviewed high-confidence country inference results into generated game data | `scripts/apply-country-inference.mjs` | `data/country-inference-preview.json`, `src/data/games.generated.ts`, `src/types/game.ts`, `package.json`, `docs/04_DATA_SCHEMA.md` | `npm run data:apply-countries` updates only `confidence: "high"` records with `countryCode`, `countryName`, and optional provenance fields; `APPLY_COUNTRIES_DRY_RUN=true` reports counts without modifying generated data |
| Country mock data    | Stores country coordinates and metadata                               | `src/data/countries.ts`                        | `src/types/game.ts`, `src/lib/localization.ts`, `docs/04_DATA_SCHEMA.md`                                                              | Country code must match games; current MVP includes the original MVP countries / regions plus country-inference countries present in generated RAWG data, including Australia, Norway, Russia, and Switzerland coordinates for globe focus / marker support. |
| Cover asset directory | Documents where future local cover images should be placed            | `public/covers/README.md`                      | `src/data/games.ts`, `src/components/globe/GameMarkers.tsx`, `src/components/panels/GameDetailCard.tsx`, `public/covers/rawg/`          | RAWG generated covers are cached under `public/covers/rawg/`; missing files fall back to existing UI placeholders |
| Filtering logic      | Filters Earth games by country, year, and rating                      | `src/lib/filterGames.ts`                       | `src/types/game.ts`, `src/types/earth.ts`                                                                                             | Earth rating range joins the shared filter pipeline without changing source game records or Chronicle filtering. Keep pure functions. |
| Statistics logic     | Computes counts, averages, top genres                                 | `src/lib/stats.ts`                             | `src/data/games.ts`, `src/types/game.ts`                                                                                              | Keep pure functions                                |
| Geographic helpers   | Normalizes connected geography, loads generated LODs, and provides deterministic renderer-neutral marker layout | `src/lib/geography.ts` | `src/lib/geographyRepository.ts`, `src/lib/markerLayout.ts`, `src/lib/geo.ts`, `scripts/generate-earth-lod.mjs`, `public/data/earth-lod/`, `tests/marker-geography-phase3.test.ts` | Stable component IDs retain antimeridian-aware rings, bounds, area, anchors and source LOD metadata. The offline RDP generator is deterministic and never overwrites the high-resolution source. |
| Region / camera helpers | Maps country codes to broad atlas regions and provides cinematic camera presets | `src/lib/regions.ts` | `src/types/game.ts`, `src/data/countries.ts`, `src/components/GameEarthApp.tsx`, `src/components/globe/GameGlobe.tsx`, `src/components/panels/CountryPanel.tsx`, `src/components/controls/BottomControls.tsx` | Owns `RegionId` configs, broad country-to-region mapping, `CameraMode` labels, closer `CAMERA_MODE_CONFIGS`, `CAMERA_PRESETS`, region labels, and pure helpers for filtering countries / games by active region without mutating source country data |
| Localization helpers | Maps UI display labels for countries, games, genres, and view modes   | `src/lib/localization.ts`                      | `src/types/game.ts`, `src/components/GameEarthApp.tsx`, `src/components/globe/`, `src/components/panels/`, `src/components/controls/` | Keeps Chinese-first product language mapping outside raw data structures |
| Search logic         | Searches countries or games                                           | `src/lib/search.ts`                            | `src/types/game.ts`                                                                                                                   | Optional in MVP                                    |
| Shared types         | Defines Game, Country, filters, view modes                            | `src/types/game.ts`                            | `docs/04_DATA_SCHEMA.md`                                                                                                              | Update schema doc if changed                       |
| Earth state types    | Defines projection, settled renderer views, filters, safe viewport, and semantic navigation intent | `src/types/earth.ts` | `src/lib/earthViewState.ts`, `src/components/GameEarthApp.tsx`, `docs/04_DATA_SCHEMA.md` | Imperative camera, controls, scene, and renderer objects are excluded from shared React state. |

## Documentation Maintenance Rule

When Codex adds a new feature, it must add a new row to this file.

When Codex moves, renames, deletes, or splits a file, it must update the relevant row.

When Codex changes a feature's responsibility, it must update the `Main File`, `Related Files`, and `Notes` columns.

## Current MVP Feature Priority

Build in this order:

1. Project setup and base layout
2. Mock data and TypeScript types
3. Main app shell
4. Country list panel
5. Map / globe view
6. Game markers
7. Country detail panel
8. Game detail card
9. Year filter
10. Cover size control
11. Visual polish
