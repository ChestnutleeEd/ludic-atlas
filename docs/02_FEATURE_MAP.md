# 02_FEATURE_MAP.md

## Purpose

This document maps each product feature to its responsible files.

Codex must read this file before modifying any feature. The goal is to avoid unnecessary full-project search and quickly locate the correct files.

## How Codex Should Use This File

Before modifying a feature:

1. Find the feature in the table below.
2. Read only the listed main file and related files.
3. Modify the smallest necessary file set.
4. If a feature or file path changes, update this document.

If the requested feature is not listed here, Codex may inspect the project structure and then add the new mapping to this file.

## Feature-to-File Mapping

| Feature              | User-Facing Behavior                                                  | Main File                                      | Related Files                                                                                                                         | Notes                                              |
| -------------------- | --------------------------------------------------------------------- | ---------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------- |
| Project setup        | App can be installed, linted, type checked, and run locally           | `package.json`                                 | `next.config.ts`, `tsconfig.json`, `postcss.config.mjs`, `eslint.config.mjs`, `package-lock.json`, `.gitignore`                       | Next.js + TypeScript + Tailwind + ESLint baseline  |
| Project documentation | User and Codex can find product positioning, setup, source entrypoints, release notes, and verification rules | `README.md`                                    | `docs/assets/readme/`, `docs/releases/`, `docs/00_PROJECT_INDEX.md`, `docs/06_CODEX_RULES.md`, `.codex_task_template.md`, `docs/05_TASK_LOG.md`                                | Keep source location, README preview screenshots, release documentation, and page acceptance guidance current |
| Home page            | User opens the app and sees the Ludic Atlas / 游戏星图 landing hub     | `src/app/page.tsx`                             | `src/components/GameEarthApp.tsx`, `src/components/home/LandingHub.tsx`, `src/app/globals.css`                                        | Keep page entry simple; initial experience is a two-entry product hub for Earth Explorer and Game Chronicle |
| Root layout          | Provides App Router document shell, Ludic Atlas metadata, favicon, and global CSS | `src/app/layout.tsx`                           | `src/app/globals.css`, `public/favicon.svg`                                                                                           | Keep layout minimal                                |
| Main product shell   | Overall layout, top-level state, data flow                            | `src/components/GameEarthApp.tsx`              | `src/data/games.ts`, `src/data/countries.ts`, `src/lib/filterGames.ts`, `src/lib/stats.ts`, `src/lib/localization.ts`, `src/components/home/LandingHub.tsx`, `src/components/globe/GameGlobe.tsx`, `src/components/archive/GameArchiveView.tsx` | Owns selected country, selected game, hovered game, earth filters, cover size, marker view mode, and main view mode (`hub` / `earth` / `archive`). Defaults to the Ludic Atlas landing hub; Earth Explorer and Game Chronicle are separate large entry surfaces |
| Landing hub         | User chooses between Earth Explorer / 地球探索, Earth Explorer Pro / 游戏地球 Pro, and Game Chronicle / 游戏编年馆 as independent product entrances | `src/components/home/LandingHub.tsx` | `src/components/GameEarthApp.tsx`, `src/app/earth-pro/page.tsx`, `src/app/globals.css`, `public/textures/walnut-wood.svg`, `public/textures/brass-noise.svg` | Initial screen uses the Ludic Atlas / 游戏星图 name, shows a large title, bilingual subtitle, legacy Earth card with planet / latitude visual treatment, Earth Explorer Pro experiment card, and Chronicle card with a wooden cabinet face, brass label, timeline ticks, drawer handles, and small cover cards |
| Earth Explorer Pro experimental entry | User opens `/earth-pro` or the Game Earth Pro card and gets a full-screen immersive MapLibre / deck.gl earth-map experiment without replacing the legacy `react-globe.gl` Earth Explorer | `src/components/earth-pro/EarthExplorerProApp.tsx` | `src/app/earth-pro/page.tsx`, `src/components/earth-pro/EarthProMap.tsx`, `src/lib/earthPro.ts`, `src/components/home/LandingHub.tsx`, `src/app/globals.css`, `src/data/games.ts`, `src/data/countries.ts`, `public/data/world-countries-lite.geojson`, `public/covers/fallback-game-cover.svg`, `package.json`, `package-lock.json`, `docs/03_ARCHITECTURE.md` | Experimental route uses MapLibre GL JS for the full-screen WebGL country basemap / globe projection and deck.gl `ScatterplotLayer`, `IconLayer`, and `TextLayer` for GPU marker rendering. It keeps local generated data only, separates total generated games from country-mapped games, uses deterministic marker caps / layouts, local-only cover textures with fallback for remote cover URLs, fixed region / key-country camera presets plus dynamic camera views for any selected mapped country, a stable non-recreated MapLibre instance with resize observers, hover tooltip, a lightweight top HUD, left-side legend / controls, and a right collapsible panel that becomes a collapsed mobile bottom sheet. |
| Game Chronicle view | User browses generated global games through a premium retro digital archive, searchable collection index, horizontal film-style year timeline, and selected-year exhibit modal with game archive details | `src/components/archive/GameArchiveView.tsx` | `src/components/archive/ArchiveTimeline.tsx`, `src/components/archive/ArchiveYearModal.tsx`, `src/components/archive/ArchiveDossier.tsx`, `src/components/archive/ArchiveYearDrawer.tsx`, `src/components/GameEarthApp.tsx`, `src/data/games.ts`, `src/types/game.ts`, `src/lib/gameCover.ts`, `src/lib/localization.ts`, `src/app/globals.css`, `public/images/archive/archive-hall-bg-v1.png`, `public/covers/rawg/`, `public/textures/archive-wood.svg`, `public/textures/archive-noise.svg`, `public/textures/archive-grid.svg`, `public/textures/walnut-wood.svg`, `public/textures/archive-paper.svg`, `public/textures/brass-noise.svg` | Groups filtered games by `releaseYear`, splits genre / platform tag strings before statistics and filtering, uses OR logic for multi-select genre and platform filters, supports `year-desc` and `rating-desc`, renders cached local covers through `getGameCoverImage` with regular `<img loading="lazy">` and fallback recovery, recomputes timeline counts and cover previews from filtered games, uses GSAP for page entry and `motion` for filter chips / year cards / modal transitions, keeps the main page focused on hero metrics, curator filters, and film-like year cards, and opens a full-screen responsive Year Exhibit modal with a default featured game plus year overview / selected game dossier. The modal uses stable exhibition cards with fixed 3:4 cover ratio, clamped accessible titles, separate year / rating badges, separate genre / platform rows, scroll-contained dossier text, Escape / backdrop close, background scroll lock, and a premium retro archive visual system built from deep navy, charcoal, brass, amber, warm ivory, restrained scanlines, grid texture, and paper dossier surfaces. |
| Legacy 2.5D earth map view  | File remains available, but there is no active UI entry in the MVP shell | `src/components/globe/GameGlobe2D.tsx`         | `src/lib/geo.ts`, `src/components/globe/GameTooltip.tsx`, `src/app/globals.css`, `public/data/mock-countries.geojson`, `src/data/countries.ts`, `src/data/games.ts` | Legacy fallback component is retained on disk to avoid wide deletion risk, but `GameEarthApp` no longer imports or renders it and the active globe UI no longer exposes a 2.5D / 3D toggle |
| 3D earth / map view  | User sees, deeply zooms, manually rotates, hovers, filters by region, and focuses a cinematic real interactive 3D earth with full world country outlines | `src/components/globe/GameGlobe.tsx`           | `src/components/globe/GameMarkers.tsx`, `src/components/globe/CountryLayer.tsx`, `src/lib/geo.ts`, `src/lib/regions.ts`, `src/lib/gameCover.ts`, `src/app/globals.css`, `public/data/world-countries-lite.geojson`, `public/data/mock-countries.geojson`, `public/data/countries.geojson`, `public/covers/fallback-game-cover.svg` | Default earth mode uses `react-globe.gl` with dynamic client loading, premium black-gold atlas material, lightweight full-world polygon outlines, country dot-matrix points, OrbitControls pan disabled, closer Surface zoom limits, damping, selected-country focus, global / focus / zoom buttons, region camera presets, country quick-focus presets for Japan / China / United States / South Korea / United Kingdom / Netherlands / Belgium / Switzerland / Denmark, Overview / Surface camera modes, no default country labels, local-only marker hover state, and drag-time marker downgrading. Bottom controls send zoom / reset / focus camera commands to the globe. Engine evaluation currently favors deeper `react-globe.gl` optimization over CesiumJS or MapLibre/deck.gl migration for the MVP. |
| Game cover markers   | Country aggregate dots and selected/high-rating game covers appear on countries / regions | `src/components/globe/GameMarkers.tsx`         | `src/lib/geo.ts`, `src/lib/regions.ts`, `src/lib/gameCover.ts`, `src/data/games.ts`, `src/data/countries.ts`, `src/components/globe/GameTooltip.tsx`, `src/lib/localization.ts`, `public/covers/README.md`, `public/covers/fallback-game-cover.svg` | Builds mixed globe HTML markers for country labels and games. Country marker mode uses lightweight country aggregate dots with counts and click-to-country behavior. Game marker mode promotes only high-rated representative games to cover cards; selected countries / selected games restore richer cover cards and tooltips. Region mode keeps a capped candidate set per country, selected-country mode shows at most 12 top-rated current-year games, and drag / zoom downgrades cover cards to dots. Marker cover images use `getGameCoverImage` and fall back to the shared SVG on missing or failed loads. Marker coordinates use deterministic country-anchor offsets plus fixed global / region / selected-country slot templates in `src/lib/geo.ts` so same-country games and dense European countries remain stable and less overlapped. |
| Country layer        | Countries can be hovered or clicked                                   | `src/components/globe/CountryLayer.tsx`        | `src/types/game.ts`, `src/lib/geo.ts`, `src/components/GameEarthApp.tsx`, `public/data/world-countries-lite.geojson`, `public/data/mock-countries.geojson` | Provides lightweight `react-globe.gl` polygon props for all world countries plus mock-country dot-matrix point props. Hover / selected states brighten only the matching country polygon / points; ocean / blank hover clears highlight. Mock country clicks update the right panel, while non-mock country clicks only highlight the globe polygon |
| Game hover tooltip   | User hovers over a game and sees title, rating, year, genre           | `src/components/globe/GameTooltip.tsx`         | `src/types/game.ts`, `src/components/globe/GameMarkers.tsx`, `src/lib/localization.ts`                                                | Tooltip receives hovered game data from props and displays Chinese-first copy |
| Right panel wrapper  | Right side switches between region-scoped country overview and selected-country detail | `src/components/panels/RightPanel.tsx`         | `src/components/panels/CountryPanel.tsx`, `src/components/panels/CountryDetailPanel.tsx`, `src/components/panels/GameDetailCard.tsx`, `src/lib/regions.ts`  | Shows searchable region-scoped country overview when no country is selected; shows selected-country detail mode after selection; exposes clear-country and game-selection callbacks; keeps long detail content inside a bounded scroll area. When `selectedGame` exists, renders a modal detail layer above the panel, marks background content inert, disables background pointer events / scrolling, and supports Escape close. On mobile the panel behaves as a fixed three-state bottom sheet (`collapsed`, `peek`, `expanded`) with a drag handle, state buttons, internal scrolling, and globe-drag collapse behavior. |
| Country list panel   | User sees compact searchable country rows with game count, average rating, and top genre | `src/components/panels/CountryPanel.tsx`       | `src/lib/stats.ts`, `src/lib/localization.ts`, `src/data/games.ts`, `src/data/countries.ts`                                           | Statistics are memoized from the filtered game list and displayed in Chinese-first compact rows. Search supports Chinese names, English names, and country codes; row clicks update selected country and trigger globe focus through app state |
| Global RAWG gallery  | User can preview batch-generated RAWG games and cached covers before choosing a country | `src/components/panels/GlobalGameGallery.tsx`  | `src/components/panels/RightPanel.tsx`, `src/types/game.ts`, `src/lib/localization.ts`, `src/data/games.generated.ts`, `public/covers/rawg/` | Appears below the country overview only when no country is selected. Uses regular `<img>` for generated `coverImage` paths; limits the preview to the first 24 games |
| Country detail panel | User clicks a country and sees a vertical country detail panel with stats and game cards | `src/components/panels/CountryDetailPanel.tsx` | `src/lib/filterGames.ts`, `src/lib/stats.ts`, `src/lib/localization.ts`, `src/lib/gameCover.ts`, `src/components/panels/GameDetailCard.tsx`                   | Uses selected country, current year-filtered games, selected game state, and Chinese-first labels. Includes return-to-overview, country stats, current-year game card flow with stable cover fallback, and a sticky bottom game summary dock |
| Game detail card     | User clicks a game and sees a closable compact description card        | `src/components/panels/GameDetailCard.tsx`     | `src/types/game.ts`, `src/lib/localization.ts`, `src/lib/gameCover.ts`, `src/data/games.ts`, `public/covers/fallback-game-cover.svg`                                                                   | Shows reusable compact game details with Chinese-first labels, real or fallback cover image, developer / publisher / year / genre / platform / rating / description, and optional close action |
| Bottom controls      | User controls filters, marker size, camera mode, zoom/reset, rotate mode, and display options  | `src/components/controls/BottomControls.tsx`   | `src/components/controls/YearSlider.tsx`, `src/components/controls/CoverSizeSlider.tsx`, `src/components/controls/ViewModeToggle.tsx`, `src/lib/regions.ts` | Shows current visible game count, country count, current region, year slider, marker size slider, Overview / Surface camera toggle, zoom in / zoom out / reset controls, rotate toggle, and marker display mode; passes native input/button updates to GameEarthApp |
| Year filter          | User filters games by release year                                    | `src/components/controls/YearSlider.tsx`       | `src/lib/filterGames.ts`, `src/components/GameEarthApp.tsx`                                                                           | Uses native range inputs; state owned by GameEarthApp |
| Cover size control   | User changes marker / cover size                                      | `src/components/controls/CoverSizeSlider.tsx`  | `src/components/globe/GameMarkers.tsx`, `src/components/GameEarthApp.tsx`                                                             | Uses native range input                            |
| View mode toggle     | User switches marker display mode                                     | `src/components/controls/ViewModeToggle.tsx`   | `src/components/globe/GameMarkers.tsx`, `src/components/GameEarthApp.tsx`                                                             | Supports countries and games modes                 |
| Game data export     | Provides representative game records to the frontend from local static modules | `src/data/games.ts`                            | `src/data/games.generated.ts`, `src/data/games.mock.ts`, `src/types/game.ts`, `src/lib/localization.ts`, `public/covers/README.md`, `public/covers/rawg/`, `docs/04_DATA_SCHEMA.md` | `games.ts` is the stable frontend import. Generated RAWG data is preferred through `games.generated.ts`; `games.mock.ts` preserves the original stable mock dataset. RAWG generated `coverImage` values may point to cached `/covers/rawg/...` files after `npm run data:covers` |
| RAWG data generation | Local script fetches RAWG data and writes a static generated game data module | `scripts/fetch-rawg-games.mjs`                 | `scripts/rawg-seeds.mjs`, `src/data/games.generated.ts`, `src/data/games.ts`, `package.json`, `.env.local`, `README.md`, `docs/03_ARCHITECTURE.md`, `docs/04_DATA_SCHEMA.md` | Browser components must not call RAWG. The script reads `RAWG_API_KEY` from `.env.local`; `RAWG_FETCH_MODE=batch` uses the RAWG `/api/games` list endpoint with date / metacritic pagination and writes `src/data/games.generated.ts`. Non-batch mode preserves the legacy seeded detail-fetch flow through `scripts/rawg-seeds.mjs` |
| RAWG cover cache     | Local script downloads generated remote RAWG cover URLs into Git-tracked static assets and rewrites `coverImage` to local paths | `scripts/cache-rawg-covers.mjs` | `src/data/games.generated.ts`, `public/covers/rawg/`, `package.json`, `docs/04_DATA_SCHEMA.md`, `docs/03_ARCHITECTURE.md` | `npm run data:covers` reads generated data, uses `undici` proxy support from shell proxy env vars, downloads covers with limited concurrency, skips existing non-empty files, keeps failed URLs unchanged, writes `/covers/rawg/...` paths back to `games.generated.ts`, and does not read `.env.local` or require a RAWG API key |
| RAWG cover compression | Local script compresses cached RAWG cover files to WebP and rewrites generated cover references | `scripts/compress-rawg-covers.mjs` | `src/data/games.generated.ts`, `public/covers/rawg/`, `package.json` | `npm run data:covers:compress` creates a timestamped backup of `games.generated.ts` plus RAWG cover file statistics under `backups/`, converts jpg/jpeg/png/webp covers in `public/covers/rawg/` to `.webp` with max dimension 1000px and quality 80, deletes old same-basename jpg/jpeg/png files after successful conversion, and verifies generated local cover references exist |
| Country inference apply | Writes reviewed high-confidence country inference results into generated game data | `scripts/apply-country-inference.mjs` | `data/country-inference-preview.json`, `src/data/games.generated.ts`, `src/types/game.ts`, `package.json`, `docs/04_DATA_SCHEMA.md` | `npm run data:apply-countries` updates only `confidence: "high"` records with `countryCode`, `countryName`, and optional provenance fields; `APPLY_COUNTRIES_DRY_RUN=true` reports counts without modifying generated data |
| Country mock data    | Stores country coordinates and metadata                               | `src/data/countries.ts`                        | `src/types/game.ts`, `src/lib/localization.ts`, `docs/04_DATA_SCHEMA.md`                                                              | Country code must match games; current MVP includes the original MVP countries / regions plus country-inference countries present in generated RAWG data, including Australia, Norway, Russia, and Switzerland coordinates for globe focus / marker support. |
| Cover asset directory | Documents where future local cover images should be placed            | `public/covers/README.md`                      | `src/data/games.ts`, `src/components/globe/GameMarkers.tsx`, `src/components/panels/GameDetailCard.tsx`, `public/covers/rawg/`          | RAWG generated covers are cached under `public/covers/rawg/`; missing files fall back to existing UI placeholders |
| Filtering logic      | Filters games by country, year, genre                                 | `src/lib/filterGames.ts`                       | `src/types/game.ts`                                                                                                                   | Keep pure functions                                |
| Statistics logic     | Computes counts, averages, top genres                                 | `src/lib/stats.ts`                             | `src/data/games.ts`, `src/types/game.ts`                                                                                              | Keep pure functions                                |
| Geographic helpers   | Converts coordinates, maps GeoJSON countries, provides dot-matrix samples, and provides globe camera / 2.5D map views | `src/lib/geo.ts`                               | `src/data/countries.ts`, `src/components/globe/GameMarkers.tsx`, `src/components/globe/CountryLayer.tsx`, `src/components/globe/GameGlobe2D.tsx` | Keeps legacy 2D projection helpers, 2.5D SVG projection / GeoJSON path helpers / viewBox focus helpers, globe marker clustering, GeoJSON Alpha-2 / Alpha-3 / name fallback mapping, country polygon dot-matrix sampling, global camera view, country-specific marker anchor offsets, fixed marker spread slots, country-specific marker spread profiles, and closer country focus camera views for current country data |
| Region / camera helpers | Maps country codes to broad atlas regions and provides cinematic camera presets | `src/lib/regions.ts` | `src/types/game.ts`, `src/data/countries.ts`, `src/components/GameEarthApp.tsx`, `src/components/globe/GameGlobe.tsx`, `src/components/panels/CountryPanel.tsx`, `src/components/controls/BottomControls.tsx` | Owns `RegionId` configs, broad country-to-region mapping, `CameraMode` labels, closer `CAMERA_MODE_CONFIGS`, `CAMERA_PRESETS`, region labels, and pure helpers for filtering countries / games by active region without mutating source country data |
| Localization helpers | Maps UI display labels for countries, games, genres, and view modes   | `src/lib/localization.ts`                      | `src/types/game.ts`, `src/components/GameEarthApp.tsx`, `src/components/globe/`, `src/components/panels/`, `src/components/controls/` | Keeps Chinese-first product language mapping outside raw data structures |
| Search logic         | Searches countries or games                                           | `src/lib/search.ts`                            | `src/types/game.ts`                                                                                                                   | Optional in MVP                                    |
| Shared types         | Defines Game, Country, filters, view modes                            | `src/types/game.ts`                            | `docs/04_DATA_SCHEMA.md`                                                                                                              | Update schema doc if changed                       |

## Documentation Maintenance Rule

When Codex adds a new feature, it must add a new row to this file.

When Codex moves, renames, deletes, or splits a file, it must update the relevant row.

When Codex changes a feature's responsibility, it must update the `Main File`, `Related Files`, and `Notes` columns.

## Current MVP Feature Priority

Build in this order:

1. Project setup and base layout
2. Mock data and TypeScript types
3. Main app shell
4. Country list panel
5. Map / globe view
6. Game markers
7. Country detail panel
8. Game detail card
9. Year filter
10. Cover size control
11. Visual polish
