# Domain docs

This is a single-context repository.

Before architecture, diagnosis, or test-driven work, read:

- `CONTEXT.md` at the repository root when it exists;
- relevant architectural decisions under `docs/adr/` when that directory exists;
- the project navigation and product vocabulary in `docs/00_PROJECT_INDEX.md`, `docs/01_PRODUCT_SPEC.md`, and `docs/02_FEATURE_MAP.md`.

Use the project's established names—Ludic Atlas, Earth Explorer, Game Chronicle, country mapping, and game records—in engineering output. Surface any conflict with an existing ADR explicitly.
